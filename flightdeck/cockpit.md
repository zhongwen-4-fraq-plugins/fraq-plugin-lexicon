# Cockpit — fraq-plugin-lexicon

Focus: 条件模式的 `[逻辑.or...]` 和 `[逻辑.and...]` 均已收紧为只接受小写 `true`、`false`；本地验证通过，尚未发布安装。

## In flight

- `template-message-plugin` — README 已有管理命令表，用法文档已有完整词条速查；目标 Fraq 仍为 `v0.3.4`，严格逻辑布尔规则待后续发布安装

## Next

- 在真实群聊中验证 ID 3 的“获取陌生人信息@用户”。
- 在真实群聊中验证 `[词库.拒绝执行]` 后的 API 和嵌套词条不执行。
- 在真实群聊中验证 `[逻辑.休眠.<秒数>]` 等待后继续执行。
- 发布并安装严格 `逻辑.or`、`逻辑.and` 布尔规则，验证别名和大小写变体不再被接受。
- 在真实群聊中验证计次循环、变量次数、`[循环.退出]` 和 `[循环.跳过]`。
- 在真实群聊中验证 JSON 变量取值、数组下标和 API 返回值解析。
- 在真实群聊中验证 `[逻辑.请求用户输入.<提示文本>.<超时时间=秒>.<超时提示=文本>]`。
- 在真实群聊中验证消息段取值和文本消息段构建模板。
- 在真实群聊中验证 `词库 查询 <词条ID>` 和 `词库 查询 <词库名> <词条ID>`。
- 在真实群聊中验证 `词库 修改 <词条ID> [问 <新问题>] 答 <新回答>`。
- 在真实群聊中验证显式逻辑条件分支、文本逻辑词条和嵌套解析。
- 在真实群聊中验证 `[逻辑.or.[变量.读取.变量名].备用文本]` 以及消息段缺失时的回退。

## Open questions

- 真实 Milky 服务是否还存在 Fraq 类型未覆盖的实现特有参数差异？
